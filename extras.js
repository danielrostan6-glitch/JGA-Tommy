window.renderHunt=()=>{let winner=localStorage.getItem('jga-winner')||'';shell(`<h1 class="section-title">Schnitzeljagd</h1><div class="card"><div class="eyebrow">Gewinner</div><div class="winner-actions"><button class="btn winner-btn ${winner==='tommy'?'winner-green':winner==='jga'?'winner-red':'btn-dark'}" onclick="setWinner('tommy')">Gewinner Tommy</button><button class="btn winner-btn ${winner==='jga'?'winner-green':winner==='tommy'?'winner-red':'btn-dark'}" onclick="setWinner('jga')">Gewinner Team JGA</button></div></div><div class="card"><div class="eyebrow">Fortschritt</div><h3>${getDone().length} / 8 erledigt</h3><div class="progress"><i style="width:${getDone().length/8*100}%"></i></div></div><div id="map" class="map"></div>${D.stations.map((s,i)=>`<div class="card station"><div class="num">${i+1}</div><div><h3>${s[0]}</h3><div class="task">${s[1]}</div><div class="actions"><a class="btn btn-outline" target="_blank" href="${s[4]||`https://www.google.com/maps/dir/?api=1&destination=${s[2]},${s[3]}`}">Route starten</a><button class="btn ${getDone().includes(i)?'btn-gold':'btn-dark'}" onclick="toggleDone(${i})">✓ ${getDone().includes(i)?'Erledigt':'Erledigen'}</button></div></div></div>`).join('')}`);setTimeout(initMap,50)};window.setWinner=w=>{localStorage.setItem('jga-winner',w);renderHunt()};function initMap(){if(!window.L)return;let m=L.map('map',{zoomControl:false}).setView([36.7202,-4.4188],15);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(m);D.stations.forEach((s,i)=>L.marker([s[2],s[3]],{icon:L.divIcon({className:'',html:`<div class="num">${i+1}</div>`,iconSize:[38,38]})}).addTo(m).bindPopup(`<b>${s[0]}</b><br>${s[1]}`))}window.renderGames=()=>shell(`<h1 class="section-title">Spiele</h1>${D.games.map(g=>`<div class="card game"><div class="eyebrow">10 Punkte</div><h3>${g[0]} ${g[1]}</h3><p class="muted">Challenge gemeinsam meistern.</p><button class="btn btn-dark" onclick="this.classList.toggle('btn-gold');this.textContent=this.classList.contains('btn-gold')?'✓ Erledigt':'Challenge starten'">Challenge starten</button></div>`).join('')}`);window.renderAdmin=()=>shell(`<div class="card admin-block"><div class="eyebrow">Admin</div><h1>Verwaltung</h1><p class="muted">Die Reisedaten liegen in data.js und können direkt im Browser über GitHub bearbeitet werden. Fortschritte werden auf dem jeweiligen Gerät gespeichert.</p><button class="btn btn-outline" onclick="localStorage.removeItem('jga-done');location.reload()">Fortschritt zurücksetzen</button></div>`,'Admin',false);